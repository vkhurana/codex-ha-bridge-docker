{
  "name": "codex-ha-bridge",
  "version": "0.1.0",
  "description": "Publish Codex usage limits to Home Assistant over MQTT.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/auth.js && node --check src/codexUsage.js && node --check src/mqttHa.js && node --check src/simpleMqtt.js"
  },
  "dependencies": {},
  "engines": {
    "node": ">=20"
  }
}
